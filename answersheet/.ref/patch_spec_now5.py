import io, sys
sys.stdout.reconfigure(encoding='utf-8')
P = '答题卡排版规格.md'
s = io.open(P, encoding='utf-8').read()
i = s.find('### 7.14 已知未解决')
j = s.find('---', i)
assert i > 0 and j > i
s = s[:i] + """### 7.14 已知未解决 / 待用户裁决

| 项 | 说明 |
|---|---|
| **几何自检 7 处不通过（存量）** | `geometry.selfTest()` 在 A4 上报 7 处：<br>① `topMark#0 cx=16.641` 不落在任何列心 —— 顶部定标带两端各贴一个角标（块心 16.641），而第一列的列心是 22.952，**设计上第 0 枚本来就不该对列心**；<br>② `topMark#25..28 cx=174.416/180.727/187.038/193.349` —— 定标带 29 枚、网格 24 列，右侧多出的 5 枚越过了最后一列的列心（195.379 是右侧角标，列心只到 164.568）；<br>③ `红框左缘 14.005 != 左侧定位点右边缘 11.005` —— 差 3.0mm。<br>这三条**与分页器重写无关**（重写前就在）。需要用户裁决：是「定标带两端本来就该贴角标、不必对列心」应放宽自检，还是 29 枚该改成 24 枚。 |
| A3 | 本轮只针对 A4（用户指定「先专注于 a4」）。A3 的 `answerTop/Bottom` 已同源，但**重写后未做探针验证**。总面数凑偶目前按「面数」补，A3 一页三面，`3` 的倍数关系未特别处理。 |
| `fitFrames()` 不进 `renderAll` 的返回值 | 它是渲染后修正，`renderAll()` 的返回值里不含这个修正量。要复核收口结果必须量 DOM（`.ref/layout.cjs` 就是这么做的）。 |
| 竖排非答题区未实测 | `fitStyle()` 的竖排分支（`h > w` 时 `writing-mode: vertical-rl`）在 A4 上几乎不会触发（框宽恒 182mm），只在人工构造的极窄高框上才会用到。逻辑在，但没跑过实例。 |
| 「贴条形码区」字体 | 用户提出应为宋体而非黑体；未改。 |
| 选考框 | 需要用户给出设计（题号、选项数、位置）。 |

""" + s[j:]
io.open(P, 'w', encoding='utf-8', newline='').write(s)
print('§7.14 已更新')
