import io, sys
sys.stdout.reconfigure(encoding='utf-8')
p = '答题卡排版规格.md'
s = io.open(p, encoding='utf-8').read()

a = s.index('### 6.6.3 选择题换行后的左侧定标带（先算数量，再对齐右边）')
b = s.index('### 6.6.4 考生信息行')

new = '''### 6.6.3 选择题换行后的左侧定标带（每行 5 块，行间不放块）

用户画的结构图 —— **这是最终口径**：

```
【方块】1 2 3 ……      ← 题号行
【方块】A A A ……      ← A 选项行
【方块】B B B ……
【方块】C C C ……
【方块】D D D ……
【方块】21 22 23 ……   ← 下一行紧接，中间没有额外的方块
【方块】A A A ……
```

**数量公式**：

```
左侧定标块数 = nLines × rowOffsets.length        （A4：nLines × 5）
```

A4 实测（40 题 = 2 行）：**10 块**（另加「缺考框」1 块，共 11）。
行与行之间看到的那个"间隙"是 `lineGapH` 把两行拉开的结果，**不是多画了一块**。

| 序号 | 块心 y | 对应行 | 与上一块间距 |
|---|---|---|---|
| — | 80.449 | 缺考框（网格外） | — |
| 1 | 100.227 | 行1 题号 | — |
| 2 | 104.969 | 行1 A | 4.742 |
| 3 | 109.260 | 行1 B | 4.291 |
| 4 | 113.547 | 行1 C | 4.287 |
| 5 | 117.835 | 行1 D | 4.288 |
| 6 | 123.164 | 行2 题号 | **5.329**（行距，无块） |
| 7 | 127.910 | 行2 A | 4.746 |
| 8 | 132.197 | 行2 B | 4.287 |
| 9 | 136.484 | 行2 C | 4.287 |
| 10 | 140.775 | 行2 D | 4.291 |

> ⚠️ **踩坑记录（六次）**：先后六次尝试在行间**塞一块**，全部失败。块高 2.436mm，
> 而行间可用净空最多只有 2.4 左右，无论怎么摆都会与相邻块**重叠或挤成一团**：
>
> | 摆法 | 结果 |
> |---|---|
> | 延续上一行节距 | 距下一行题号块 1.04 → **叠** |
> | 「末气泡下缘 / 题号盒上缘」取中点 | 距上一行末尾 1.66 → **挤** |
> | 用几何口径的题号块心取中点 | 距题号块 0.09 → **叠** |
> | 换 `NUM_BIAS` 各种符号（3 次） | 距题号块 0.97 / 1.05 → **叠** |
>
> 用户原话：「你怎么方块融合在一起了？有这么难理解吗？？？」
> **结论：行间不放块。** `lineGapMarkOffset()` 保留但返回 `null`，
> 只为把这段结论留在代码里。

> ⚠️ 另一个坑：`NUM_BIAS` 曾在 `numRowH()` 定义**之前**用 `var` 求值
> （`var` 提升但赋值不提升），得到 `NaN`，于是 `base + NaN` 让一整块跑到
> y = 1.218（纸顶）。已删除该变量。

**`lineGapH`（`js/geometry.js`）**：

| 量 | 值（A4） | 说明 |
|---|---|---|
| `lineGapH` | **2.436** = `n · bubbleH` | 行顶到行顶里扣掉列高之后的部分 |
| `lineHeight` | 22.939 = `columnHeight + lineGapH` | |
| `rowPitch` | 4.288 = `colGap + bubbleH` | 气泡行之间的行节距（行心距） |
| `rowOffsets` | 1.6755, 6.421, 10.709, 14.997, 19.285 | 题号块 + A/B/C/D 的块心 |

`lineGapH = n·bubbleH` 是**实测标定**出来的：`.ref/cal_gap.cjs` 把网格
`row-gap` 强制成 4mm / 8mm 各量一次，「上行末气泡**底缘** → 下行题号**上缘**」
的净空正好等于该 row-gap（**斜率 1、截距 0**）。`columnHeight` 恰好停在
末气泡底缘，题号上缘恰好落在下一行块顶，中间**不需要任何补偿项**。

> ⚠️ 曾把 `.as-choice-nums` 的**外层盒**（transform 之前的位置）误当成题号上缘，
> 量出 3.69mm 的假净空，于是加了「修正」，把 `lineGapH` 压到 **−0.915mm** ——
> 两行直接叠在一起、左侧定位点全部错位。
> **要量就量 `.as-choice-num`（span）自己的 rect，不要量父盒。**

**仍未统一的一处**（已知、暂不改）：`题号→A` 是 **4.746**，比 `rowPitch` 4.288
大 0.46mm。原因是题号字号 12.5pt 的渲染行盒（Chrome 吸附后 3.1755mm）比
`numRowH`（3.351mm）矮 0.176mm。要让这一段也等于 4.288，只能把整个气泡节距
放宽到 4.746（`colGap: 1.852 → 2.31`），一行选择题会因此变高约 5.5mm。

实测（`.ref/leftband.cjs`，40 题 = 2 行）：

```
网格定位点数量 = nLines·5 = 10，实测 10                     OK
相邻块最小间距 4.287 > 块高 2.436                          不重叠 OK
全部块心与「按数量生成」的期望值最大偏差 = 0.002 mm          OK
题号/气泡行心到最近左侧块心的最大距离 = 0.089 mm             OK
行1 末气泡底缘 118.967 → 行2 题号上缘 121.404  净空 2.437    OK（要求 2.436）
```

'''
s = s[:a] + new + s[b:]

# 页脚颜色
old_f = '| `TYPE.pageTip` | `js/config.js` | 宋体 10pt accent |'
new_f = ('| `TYPE.pageTip` | `js/config.js` | 宋体 10pt accent |\n'
         '| `TYPE.footer` | `js/config.js` | 宋体 9pt **accent（红色）** —— 用户指定；'
         '黑白主题下 accent 退化成黑，不会出红 |')
if old_f in s:
    s = s.replace(old_f, new_f, 1)
    print('footer row added')

io.open(p, 'w', encoding='utf-8', newline='').write(s)
print('spec ok')
