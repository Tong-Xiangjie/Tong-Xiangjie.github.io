import io, sys
sys.stdout.reconfigure(encoding='utf-8')
P = '答题卡排版规格.md'
s = io.open(P, encoding='utf-8').read()
n = 0
def rep(old, new, tag):
    global s, n
    assert old in s, 'MISS: ' + tag
    s = s.replace(old, new, 1); n += 1; print('  ok:', tag)

# ── §7.1.1 ──────────────────────────────────────────────────────────────────
rep("""#### 7.1.1 红框可达范围 ≠ 答题区 ★

用户 2026 追加：「红色边框上缘可以往上拉 **0.7 个黑色定位点高度**，
下缘可以往下拉 **0.7 个**」。

```
frameExtend(p)      = 0.7 × cornerH                A4: 0.7 × 4.23 = 2.961
frameTopLimit(p)    = answerTop    − frameExtend   A4: 10.154
frameBottomLimit(p) = answerBottom + frameExtend   A4: 286.846
```

**两个概念必须分开**：

| | 值（A4） | 谁能越过 |
|---|---|---|
| 答题区 `answerTop..answerBottom` | 13.115 .. 283.885 | **内容**不许越过（题号、气泡、答题行） |
| 红框可达 `frameTopLimit..frameBottomLimit` | 10.154 .. 286.846 | **红框**可以长到这里，再多就会压到角落定位点 |

`paginator` 的容量计算一律用 `F_BOT`（不再是 `A_BOT`），
收口的 `bottomLimit` 也改成 `F_BOT`；`A_TOP/A_BOT` 只用来定义
「内容区」的语义和 `fitsNext` 的参照。

实测（`.ref/ext_check.cjs`）：

```
答题区   13.115 .. 283.885
红框可达 10.154 .. 286.846     上界 − 上角标下缘 = −2.961
                               下界 − 下角标上缘 = +2.961
红框渲染 13.114 .. 286.846（第 2 面起；第 1 面顶端受页眉限制）
```

> ⚠️ 第 1 面的红框上缘达不到 10.154 —— 页眉（标题/信息/注意事项）本身
> 占到 82.95，红框只能从页眉底下开始。上拉余量在**续排面**才用得上。""",
"""#### 7.1.1 红框可达范围 —— 相对答题区**内缩** 0.7 个定位点高 ★

**用户 2026 最终口径**（推翻了此前一版「往上拉 / 往下拉」的外扩写法）：

> 「现在又需要上面往下 0.7，下面往上 0.7（个定位点高度）。
>  你不如把这个高度写死了。」

即**内缩**：红框上缘比答题区上缘**低** `0.7×cornerH`，下缘比答题区下缘**高**
同样的量。红框高度因此是一个**与页眉、与题量无关的常量**。

```
FRAME_INSET_K       = 0.7
frameExtend(p)      = 0.7 × cornerH                    A4: 2.961
frameTopLimit(p)    = answerTop    + frameExtend       A4: 16.076
frameBottomLimit(p) = answerBottom − frameExtend       A4: 280.924
frameHeight(p, H)   = frameBottomLimit − frameTopLimit A4: 264.848
```

| | 值（A4） | 语义 |
|---|---|---|
| 答题区 `answerTop..answerBottom` | 13.115 .. 283.885 | 上/下角标的内缘之间 |
| 红框固定缘 `frameTopLimit..frameBottomLimit` | 16.076 .. 280.924 | 红框**天然**的上下限 |
| 红框高度 `frameHeight` | 264.848 | **常量**，不随内容变 |

`paginator` 的容量计算一律用 `F_BOT`（不再是 `A_BOT`），
收口的 `bottomLimit` 也改成 `F_BOT`；`A_TOP/A_BOT` 只用来定义
「内容区」的语义。

> ⚠️ 第 1 面的红框上缘达不到 16.076 那么高 —— 页眉（标题/信息/注意事项）
> 本身占到 82.95，红框只能从 `页眉底 + FRAME_TOP_GAP(2.0) = 84.949` 开始。
> 16.076 在**续排面**（页眉为空）才用得上。
>
> ⚠️ 历史坑（别再走回去）：
> ① `base` 曾写成 `max(A_TOP, chromeFirst)` —— 上缘被钉死、只有下缘能动，用户报
>    「红框整体又往下偏了」；
> ② 改成 `chromeFirst − frameExtend` —— 红框高度跟着页眉漂，用户报
>    「你不如把这个高度写死了」；
> ③ 让红框高度**随内容长** —— 于是「红框底」只能靠线性回归猜，而 device px
>    吸附让偏移跨度 17mm，拟合不出（最大残差 12.8mm）。现在高度是常量，
>    「红框底 = base + frameHeight」是恒等式。

实测（`.ref/layout.cjs`，`offsetTop/offsetHeight`，不受舞台缩放影响）：

```
答题区   13.115 .. 283.885
红框固定 16.076 .. 280.924     高 264.848
首页红框上缘 84.949（= 页眉底 82.949 + FRAME_TOP_GAP 2.0）
续排面红框上缘 16.076
```""", '§7.1.1')

# ── §7.4 页脚：整段替换 ─────────────────────────────────────────────────────
i = s.find('### 7.4 页脚固定在下方定位点中心')
j = s.find('### 7.5 收口')
assert i > 0 and j > i, 'MISS §7.4 区段'
print('  §7.4 区段长度', j - i)
s = s[:i] + """### 7.4 页脚固定在下方定位点中心 ★

用户 2026 最终口径：

> 「至于页脚，应该固定在下方定位点中心的高度，而非与红色边框锚定。」

**实现方式（由构造保证，不依赖块高）：**

```
页脚块的 top       = 下定位点行心 y = H − TOP_BAND_CY = 297 − 11 = 286mm
页脚块的 transform = translateY(-50%)     ← 把块**中线**压到 286
```

这样「页脚块心 = 下角标行心」是**恒等式**，与字体度量、行数、语言都无关。
（此前的 `bottom = 8.54mm` 写法依赖把块高当成 4.92mm，而真实块高是 5.027mm，
于是页脚中线比行心低 **0.132mm**。）

实测（`.ref/foot2.cjs`，关掉舞台缩放后用 `getBoundingClientRect` 量）：

```
下角标   top 283.886  h 4.229  cy 286.001
页脚     top 283.543  h 4.920  cy 286.003
块心差   = 0.002mm
页脚底缘 288.463 ⇒ 距纸底 8.537mm
```

> ⚠️ 页脚**不再**与红框下界有任何锚定关系。红框下界的下限定在
> `geometry.frameBottomLimit(p, H)`，两者已完全解耦。
>
> ⚠️ CSS 里不要写 `calc(var(--a) − var(--b))` —— 实测 Chrome 会把整条
> 声明丢弃，页脚退回静态位置跑到纸顶。

""" + s[j:]
n += 1

# ── §7.5 收口 ───────────────────────────────────────────────────────────────
rep("""### 7.5 收口：红框底钉死在目标下界

一面排完正文后：

| 剩余空白 | 处理 |
|---|---|
| ≥ `NOANSWER_MIN_H`（15mm） | 画**非答题区**（独立红框）吃掉它 |
| < 15mm | 不画非答题区；把**最后一道非选择题的答题区**加高，黑框底边随之变高、红框底边跟着下移，空白留在答题区底部（用户明确允许） |""",
"""### 7.5 收口：红框底钉死在目标下界

用户口径（**逐字**）：

> 「非最后一面：≥15mm 画非答题区，<15mm 不画，但是要把红框的底部拉到
>  要求的高度，黑框相应移动，答题区底部多一点空白（这是允许的）」

一面排完正文后：

| 剩余空白 `rest` | 处理 |
|---|---|
| ≥ `NOANSWER_MIN_H`（15mm） | 画**非答题区**（独立红框）吃掉它；`stretch = 0` |
| < 15mm | 不画非答题区；`stretch = rest × STRETCH_K` 加高**最后一段的答题区**，黑框底边随之变高、红框底边跟着下移，空白留在答题区底部（用户明确允许） |

> ⚠️ 判据 `rest = F_BOT − cursor − frameHeight`（`frameHeight` 是常量，
> 所以这是恒等式，不是估计）。**不是**一律下拉 —— 一律下拉会把中继面
> 也强行撑满，红框/黑框被拉长，看着像排版错了。""", '§7.5 判据')

io.open(P, 'w', encoding='utf-8', newline='').write(s)
print('total', n)
