import io, sys
sys.stdout.reconfigure(encoding='utf-8')
P = '答题卡排版规格.md'
s = io.open(P, encoding='utf-8').read()
n = 0

def rep(old, new, tag):
    global s, n
    assert old in s, tag
    s = s.replace(old, new, 1)
    n += 1
    print('  ok:', tag)

rep("""`TOP_BAND_CY = 11`，`cornerH = 4.23`。实现在 `js/geometry.js` 的
`answerTop()` / `answerBottom()` / `answerHeight()`，`js/paginator.js`
只读这三个函数，不再自己算。
""",
"""`TOP_BAND_CY = 11`，`cornerH = 4.23`。实现在 `js/geometry.js` 的
`answerTop()` / `answerBottom()` / `answerHeight()`，`js/paginator.js`
只读这三个函数，不再自己算。

#### 7.1.1 红框可达范围 ≠ 答题区 ★

用户 2026 追加：「红色边框上缘可以往上拉 **0.7 个黑色定位点高度**，
下缘可以往下拉 **0.7 个**」。

```
frameExtend(p)      = 0.7 × cornerH                A4: 0.7 × 4.23 = 2.961
frameTopLimit(p)    = answerTop    − frameExtend   A4: 10.154
frameBottomLimit(p) = answerBottom + frameExtend   A4: 286.846
```

**两个概念必须分开**：

| | 值（A4） | 谁能越过 |
|---|---|---|
| 答题区 `answerTop..answerBottom` | 13.115 .. 283.885 | **内容**不许越过（题号、气泡、答题行） |
| 红框可达 `frameTopLimit..frameBottomLimit` | 10.154 .. 286.846 | **红框**可以长到这里，再多就会压到角落定位点 |

`paginator` 的容量计算一律用 `F_BOT`（不再是 `A_BOT`），
收口的 `bottomLimit` 也改成 `F_BOT`；`A_TOP/A_BOT` 只用来定义
「内容区」的语义和 `fitsNext` 的参照。

实测（`.ref/ext_check.cjs`）：

```
答题区   13.115 .. 283.885
红框可达 10.154 .. 286.846     上界 − 上角标下缘 = −2.961
                               下界 − 下角标上缘 = +2.961
红框渲染 13.114 .. 286.846（第 2 面起；第 1 面顶端受页眉限制）
```

> ⚠️ 第 1 面的红框上缘达不到 10.154 —— 页眉（标题/信息/注意事项）本身
> 占到 82.95，红框只能从页眉底下开始。上拉余量在**续排面**才用得上。
""", '7.1.1')

rep("""### 7.4 页脚进红框

用户选 (a)：页脚移到**红框内底端**。

```
页脚底缘 = answerBottom − FOOTER_PAD(0.5)      A4: 283.385
页脚块高 = FOOTER_H = 5.0
```

⇒ 每面红框的**目标下界** = `answerBottom − FOOTER_H − FOOTER_PAD` = **278.385**。
""",
"""### 7.4 页脚固定在下方定位点中心 ★

用户 2026 改口径：「页脚应该**固定在下方定位点中心的高度**，
而非与红色边框锚定。」

```
下方定位点行心 markCy = H − TOP_BAND_CY          A4: 297 − 11 = 286
页脚块高  FOOTER_BLOCK_H = 4.92                  （--fs-footer 9pt × line-height 1.55，实测）
页脚底缘 = markCy + FOOTER_BLOCK_H / 2           A4: 288.46
bottom   = H − 页脚底缘                          A4: 8.54mm
```

实测（`.ref/footer_probe.cjs` / `.ref/ext_check.cjs`）：

```
下角标 283.886..288.115   心 = 286.001
页脚   283.547..288.467   中线 = 286.007       误差 0.006mm
页脚内容：数学　第 1 面（共 2 面）   一行，12px
```

> ⚠️ **页脚与红框已解耦**：红框的 `bottomLimit` 改成
> `frameBottomLimit = 286.846`，不再减页脚块。
> 因此**红框下缘会盖住页脚上半部分**（红框 286.846 vs 页脚顶 283.547，
> 重叠 3.3mm）—— 这是两条要求叠加的必然结果：
> 「红框下缘可下拉 0.7 个定位点」+「页脚钉在下定位点中心」。
> 页脚是绝对定位、红框是 `background:#fff` 的不透明底；
> 若发现页脚被盖住，把 `.as-footer` 的 `z-index` 提到红框之上即可。
""", '7.4')

rep("""### 7.13 收口收敛实测（目标下界 278.385）

| 配置 | 红框底 / 非答题区底 | 该面用什么填 |
|---|---|---|
| 12 选择 + 2 题 | 278.425 | 拉大最后一行答题区 |
| 0 选择 + 2 题 | 非答题区 278.384 | 非答题区（h 28.9） |
| 40 选择 + 2 题 | 278.483 | 拉大最后一行答题区 |
| 0 选择 + 3 题 | 278.317 | 拉大最后一行答题区 |
| 12 选择 + 1 题 | 非答题区 278.384 | 非答题区（h 91.5） |
| 40 选择 + 1 题 | 非答题区 278.388 | 非答题区（h 68.6） |
| 0 选择 + 1 题 | 非答题区 278.384 | 非答题区（h 118.8） |""",
"""### 7.13 收口收敛实测（目标下界 = frameBottomLimit = 286.846）

| 配置 | 红框底 / 非答题区底 | 该面用什么填 |
|---|---|---|
| 12 选择 + 2 题 | 286.871 | 拉大最后一行答题区 |
| 0 选择 + 2 题 | 非答题区 286.846 | 非答题区（h 37.3） |
| 40 选择 + 2 题 | 286.933 | 拉大最后一行答题区 |
| 0 选择 + 3 题 | 286.772 | 拉大最后一行答题区 |
| 12 选择 + 1 题 | 非答题区 286.846 | 非答题区（h 99.95） |
| 40 选择 + 1 题 | 非答题区 286.846 | 非答题区（h 77.0） |
| 0 选择 + 1 题 | 非答题区 286.846 | 非答题区（h 127.3） |""", '7.13')

rep("""凑偶验证（300 选择题 + 2 题）：`共 4 张 / 4 面 · 含 1 面非答题区（凑偶数）`，
面 4 整面一个非答题区（顶 13.114、底 278.384）。""",
"""凑偶验证（300 选择题 + 2 题）：`共 4 张 / 4 面 · 含 1 面非答题区（凑偶数）`，
面 4 整面一个非答题区（顶 13.114、底 286.846）。""", '凑偶')

io.open(P, 'w', encoding='utf-8', newline='').write(s)
print('spec:', n, 'replacements; bytes', len(s.encode('utf-8')))
