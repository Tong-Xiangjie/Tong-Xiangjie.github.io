import io, sys
sys.stdout.reconfigure(encoding='utf-8')
P = '答题卡排版规格.md'
s = io.open(P, encoding='utf-8').read()

# ── 6.6.4 改成「不要班级」 ─────────────────────────────────────────────
old = """### 6.6.4 考生信息行：姓名 / 班级 / 准考证号 之间的水平空隙

用户指定：「姓名的横线和班级之间要留一定空隙，班级的横线与准考证号同理」。

| | 值 |
|---|---|
| .as-info-row gap | **5mm**（统一，五处净空实测 4.998–4.999） |
| .as-info-fill 宽 | 30mm（name / seat 各一） |"""
new = """### 6.6.4 考生信息行：姓名 / 准考证号 之间的水平空隙

> ⚠️ **用户 2026 最终澄清：信息行不要「班级」。**
> 参照件 `.ref/数学答题卡A4（6）.pdf` 第 1 面的信息行也只有
> 「姓名：」+ 横线 + 「准考证号」+ 书写栏。`js/builders/page.js` 里那对
> `<span class="as-info-label">班级：</span><span class="as-info-fill seat"></span>`
> 已删除；`css/card.css` 的 `.as-info-fill.seat` 规则保留但不再被引用。

用户指定：「姓名的横线和班级之间要留一定空隙，班级的横线与准考证号同理」。
（「班级」去掉后，这条只剩「姓名横线 ↔ 准考证号」一处空隙。）

| | 值 |
|---|---|
| .as-info-row gap | **5mm**（统一） |
| .as-info-fill 宽 | 30mm（只剩 name 一个） |"""
assert old in s, '6.6.4'
s = s.replace(old, new, 1)

# ── 追加新章节 ─────────────────────────────────────────────────────────
s = s.rstrip() + """


---

## 7. 答题区模型（用户 2026 定稿）★

全卡纵向只有**一个**答题区，它的上下界由**四角定位块**直接给出 ——
这是「对齐由构造保证」在纵向上的对应物。

### 7.1 上下界

| | 定义 | A4 实测 |
|---|---|---|
| 上界 `answerTop` | 上角标**下边缘** = `TOP_BAND_CY + cornerH/2` | **13.115**（实测 13.114） |
| 下界 `answerBottom` | 下角标**上边缘** = `H − TOP_BAND_CY − cornerH/2` | **283.885**（实测 283.886） |

`TOP_BAND_CY = 11`，`cornerH = 4.23`。实现在 `js/geometry.js` 的
`answerTop()` / `answerBottom()` / `answerHeight()`，`js/paginator.js`
只读这三个函数，不再自己算。

### 7.2 红框离角标的净空

用户要求「这个距离小一点」，取 **0.5mm**（`TIP_GAP`）：

| 量 | 值 |
|---|---|
| 红框上内边距 / 下内边距 | `FRAME_PAD = 0.5mm` |
| 提示语句 ↔ 黑框（及红框）净空 | `TIP_GAP = 0.5mm` |
| 提示语句固定高 | `TIP_H = 4.0mm` |

⇒ 红框「上装饰」= `padY` = 0.5（栏目头/上提示本身的高度由内容带出来，
**不要**在这里重复加，加了会把红框算高一整份 `headerH`）。

> ⚠️ 参照件实测「提示 → 黑框外缘」= 1.36mm，本站旧值 1.996mm。
> 现在 `--subj-tip-gap = 0.5`，并且 `.as-subject-page-tip` 的 `margin`
> **只由这一个变量给** —— 原来还有第二条重复的
> `.as-subject-page-tip { margin: 2mm 0 }` 把它覆盖成 2mm，
> 再与容器的 padding 叠加成 2.5mm，正是用户说的「留空比较多」。该重复规则已删。

### 7.3 版块间距

| 位置 | 值 | 由谁给 |
|---|---|---|
| 信息区 ↔ 注意事项 | 有（**用户确认不用改**） | `.as-head` 的行网格，页眉内部 |
| 页眉/注意事项 → 选择题 | `SECTION_GAP = 5mm` | `paginator` 的游标 |
| 选择题 → 非选择题 | `SECTION_GAP = 5mm` | `paginator` 的游标 |
| 非选择题 题 ↔ 题 | 0（同一张黑框表格内） | 表格行 |

> ⚠️ 历史 bug：`paginator` 只在「本面第一个块是选择题」时留了一次间距，
> 「选择题 → 非选择题」那处**完全没加**。现在用 `lastKind === 'choice'` 判断。

### 7.4 页脚进红框

用户选 (a)：页脚移到**红框内底端**。

```
页脚底缘 = answerBottom − FOOTER_PAD(0.5)      A4: 283.385
页脚块高 = FOOTER_H = 5.0
```

⇒ 每面红框的**目标下界** = `answerBottom − FOOTER_H − FOOTER_PAD` = **278.385**。

### 7.5 收口：红框底钉死在目标下界

一面排完正文后：

| 剩余空白 | 处理 |
|---|---|
| ≥ `NOANSWER_MIN_H`（15mm） | 画**非答题区**（独立红框）吃掉它 |
| < 15mm | 不画非答题区；把**最后一道非选择题的答题区**加高，黑框底边随之变高、红框底边跟着下移，空白留在答题区底部（用户明确允许） |

> ⚠️ 用户原话：「低于 15mm 请你将这块区域用答题区填充，即你可以直接将
> 红黑框往下拉，答题区可以留白。」以及「黑框跟着动」——
> 所以**不是**给红框加下内边距（那会让黑框与红框脱开），
> 而是加高**黑框内**最后一行 `.as-subj-body`。

### 7.6 为什么收口必须用**闭环**而不是公式

分页器的游标是纯 JS 算的，红框底是 DOM 渲染出来的。浏览器按
device px（= `1/3.7795 = 0.2646mm`）吸附每一行的盒高，
量化误差随「本面排了几行」变化。

`.ref/fit2.cjs` 在 36 组配置（0/12/40 选择题 × 1~4 题 × 6/8/10 行）上实测
「红框底 − 游标」：

```
 0 选择题：−1.895 ~ −7.234
12 选择题：+9.102 ~ +10.102
40 选择题：+7.426 ~ +10.104
```

跨度 17mm，任何线性式都拟合不出来（三元回归最大残差 **12.8mm**）。
所以 `js/app.js` 的 `fitFrames()` 改成**闭环**：

1. 先按分页器给的下拉量渲染；
2. **量**真实红框底与目标下界的差 `delta`；
3. 把 `delta / STRETCH_EFF` 补到最后一行 `.as-subj-body` 的高度上；
4. 重复 4 轮（因为「答题区加高 1mm → 红框底下移 0.75mm」，
   一轮只吃掉 75% 的误差）。

`STRETCH_EFF = 0.75` 由 `.ref/calib_stretch.cjs` 扫 S=0/1/2/3/4/6 得出
（Δy/S 在 S≥3 后稳定在 0.7503）。

实测收敛结果（目标 278.385）：

| 配置 | 红框底 | 误差 |
|---|---|---|
| 12 选择 + 2 题 | 278.425 | +0.04 |
| 0 选择 + 2 题（非答题区） | 278.384 | −0.00 |
| 40 选择 + 2 题 | 278.483 | +0.10 |
| 0 选择 + 3 题 | 278.317 | −0.07 |
| 12 选择 + 1 题（非答题区） | 278.384 | −0.00 |

### 7.7 非答题区（`js/builders/noanswer.js`）

| | 值 |
|---|---|
| 文字 | `考生请不要在此区域作答` |
| 框 | 红色圆角空心框，`border: 0.3mm solid var(--accent)`，`border-radius: 3mm`，白底 |
| 位置 | `left/right: var(--outer-left)`（与选择题/非选择题红框同宽） |
| 高度 | 分页器算好，**内联写死**（不走 CSS 流） |
| 字号 / 排向 | `app.js` 的 `fitNoAnswer()` 在 DOM 落地后按 `clientWidth/clientHeight` 实测再定 |
| 横排（`w ≥ h`） | `fontSize = min(w/11×0.85, h×0.7)` |
| 竖排（`h > w`） | `fontSize = min(h/11×0.85, w×0.7)`，`writing-mode: vertical-rl` |
| 字号夹取 | 12 ~ 60px |

> ⚠️ 必须量**内容盒**（`clientWidth/clientHeight`），不是 borderBox ——
> 边框与内边距已经占掉一部分，用 borderBox 会让字溢出。

### 7.8 总面数凑偶

用户指定：「一般要凑齐偶数页，所以不应该出现奇数页，空白的地方全部用非答题区」，
多出来的那一面**整面就是一个非答题区**（含页脚、四角定位点，页眉只留「第 N 面」）。

实现在 `js/app.js`：`Pager.paginate()` 之后判 `faces.length % 2 === 1`，
是则 push 一个 `blank: true` 的面，`noAnswerTop = answerTop`、
`noAnswerH = answerBottom − answerTop`（整面铺满）。
`js/builders/page.js` 的 `chromeOther(meta, blank)` 在 `blank` 时不印
「xx答题卡」那行标题。

> ⚠️ `sheetCount` 必须在 faces **补齐之后**再算 —— 原来在 `paginate()` 之后
> 立刻算，凑出来的那面落不进 `for` 循环，预览里看不到。

### 7.9 选择题续排面的提示语句字号

用户指定：选择题区那句「请在各题目的答题区域内作答，超出黑色矩形边框限定
区域的答案无效！」（续排面的栏目头）字号必须与非选择题区那句**完全一致**。

`.as-choice-header-no-example .as-section-tip` 的 `font-size` 从
`--fs-noteBody`（8pt）改成 **`--fs-pageTip`（10pt）**。

### 7.10 参照件实测骨架

`.ref/参照件实测骨架.md` 记录了 `数学答题卡A4（6）.pdf` 的四角定位块、
红框、黑框、页脚、各文字的 mm 坐标（A4，从纸顶量）。

| 元素 | 参照件 | 本站（当前） |
|---|---|---|
| 左上角标 | `x 5.82..12.17, y 13.76..17.57` | 行心 11，`y 8.884..13.114` |
| 答题区 | `17.57 .. 279.77`（高 262.20） | `13.114 .. 283.886`（高 270.77） |
| 提示 → 黑框 | 1.36 | 0.5 |
| 页脚 | 281.52 / 282.83 / 284.35 | 278.47..283.39 |

> 参照件的角标位置与本设计**不同**（它是 13.76 起，本设计是 8.884 起）——
> 因为本设计要求「答题区上抵上角标下边缘」，而参照件把角标放得更靠下。
> **以本设计为准**，参照件只用来核对「提示语句与框的净空」这类相对量。

### 7.11 已知未解决

| 项 | 说明 |
|---|---|
| 12 选择 + 1 题时非答题区与上方红框重叠 | 实测红框底 191.5、非答题区顶 186.894，重叠 4.6mm。分页器在「剩余 ≥ 15mm」的分支里仍会给最后一块红框加 `stretch`，两处叠加。修法：`noAnswer` 为真时把该面的 `stretch` 强制归零并重排。 |
| A3 | 本轮只针对 A4（用户指定「先专注于 a4」）。A3 的 `answerTop/Bottom` 已同源，但未做探针验证。 |
| `fitFrames()` 未进入 `renderAll` 的纯函数路径 | 它是渲染后修正，`renderAll()` 的返回值里不含这个修正量，所以 `.ref/` 里若直接读 `AS.__faces` 得到的仍是未修正的坐标。 |
"""

io.open(P, 'w', encoding='utf-8', newline='').write(s)
print('规格文档已更新，总字节', len(s.encode('utf-8')))
