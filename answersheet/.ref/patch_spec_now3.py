import io, sys
sys.stdout.reconfigure(encoding='utf-8')
P = '答题卡排版规格.md'
s = io.open(P, encoding='utf-8').read()

# ── §7.11 ──────────────────────────────────────────────────────────────────
i = s.find('### 7.11 收口分流（`noAnswer` 二选一）')
j = s.find('### 7.12 ')
assert i > 0 and j > i
s = s[:i] + """### 7.11 收口分流（`noAnswer` 二选一）

`paginator` 在收口处只做**一次**判定，`app.js` 的 `fitFrames()` 按同一个
标志分流，**不允许两处叠加**：

```
rest = F_BOT − cursor − frameHeight        // frameHeight 是常量（A4 264.848）
if (rest >= NOANSWER_MIN_H 15) { 画非答题区；该面 stretch = 0 }
else                           { 不画；stretch = rest × STRETCH_K }
```

`fitFrames()`：
- 本面有 `.as-noanswer` → 把它钉到 `frameBottomLimit`；
- 本面没有 → 量出真实 `contentBottom = max(黑框表格底, 下提示底)`，
  红框高 = `contentBottom + 下内边距 + 下边框`，再夹到 `frameBottomLimit`。

> ⚠️ **踩过的三个坑**：
> ① 在 `rest` 里又加了一份已决定的 `stretch` —— 结果红框底被估得过高、
>    `rest` 偏小，12 选择 + 1 题（真实剩余 86mm）被误判成「不够 15mm」，
>    本该画非答题区却去拉大答题区。`rest` 的判据必须是**未下拉**的量。
> ② `fitFrames()` 早期版本对「有非答题区的面」也去拉最后一行答题区，
>    导致非答题区顶低于红框底，重叠 4.6mm。
> ③ 曾把 `rest` 判据写成「一律下拉」（不看 15mm）—— 中继面被强行撑满，
>    红框/黑框被拉长，看着像排版错了。用户口径是
>    「**非最后一面**：≥15mm 画非答题区，<15mm 不画」。

""" + s[j:]

# ── §7.13 ──────────────────────────────────────────────────────────────────
i = s.find('### 7.13 收口收敛实测')
j = s.find('### 7.14 ')
assert i > 0 and j > i
s = s[:i] + """### 7.13 收口实测（目标下界 = frameBottomLimit = 280.924）

`.ref/layout.cjs`，7 组关键配置（`offsetTop/offsetHeight` 量测）：

| 配置 | 红框 top .. bot | 黑框 top .. bot | 该面用什么填 |
|---|---|---|---|
| 12 选择 + 2 题 | 124.354 .. 249.766 | 131.233 .. 244.475 | 红框自然收口 |
| 0 选择 + 2 题 | 84.931 .. 210.343 | 91.810 .. 205.052 | 红框自然收口 |
| 40 选择 + 2 题 | 147.108 .. 272.520 | 153.987 .. 267.229 | 红框自然收口 |
| 0 选择 + 3 题 | 84.931 .. 259.027 | 91.810 .. 253.735 | 红框自然收口 |
| 12 选择 + 1 题 | 124.354 .. 193.146 | 131.233 .. 187.854 | 红框自然收口 |
| 0 选择 + 1 题 | 84.931 .. 153.723 | 91.810 .. 148.431 | 红框自然收口 |
| 20 选择 + 4 题 | 124.354 .. 249.766 | 131.233 .. 244.475 | 红框自然收口 |

**7 组全部满足 `黑框底 ≤ 红框底`**（此前是黑框整条溢出红框 10.8mm）。

凑偶验证（300 选择题 + 2 题）：`共 4 张 / 4 面`，面 4 整面一个非答题区
（顶 16.140、底 280.924，高 264.848 = `frameHeight`）。
选择题续页提示与非选择题提示字号一致（都是 `13.3333px` = 10pt）。

""" + s[j:]
io.open(P, 'w', encoding='utf-8', newline='').write(s)
print('§7.11 + §7.13 已更新')
